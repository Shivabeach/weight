/** @format */

// @prepros-prepend fiberous.js

//function calss(fibs) {
//    if (fibs.SoluableFiber > 6) {
//        return `
//        <li> ${fibs.name} - ${fibs.SoluableFiber} </li>
//        `;
//    }
//}
//document.querySelector('.showed').innerHTML = `${fibers.map(calss).join('')}`;
//const overAverage = fibers.filter((fibs) => fibs.SoluableFiber > 6);
//fibers.forEach(showedd);

function showedd() {
    if (fibers.SoluableFiber > 6) {
        document.querySelector('.showed').innerHTML = `<li>${fibers.name} - ${fibers.SoluableFiber}</li>
    `;
    }
}
showedd();
